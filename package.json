{
  "name": "engram-sdk-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Engram developer ecosystem — TypeScript SDK, logger, and WAL",
  "type": "module",
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
